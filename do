总体判断
  当前 FlutterGuard 已可用于真实项目的本地检查、辅助代码评审和选择性 CI 门禁，尤其适合单包、标准 lib/** 布局的 Flutter/IoT 项目。

  但规则成熟度差异较大。它本质上是“AST 语法扫描 + 文本启发式”，不是类型分析器、依赖漏洞扫描器或安全审计工具。

  主要检测场景

   场景                                                 对应规则                                       当前可用性
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   大文件、大类、过长 build()                           large_file、large_class、large_build_method    较成熟，调好阈值后可门禁
  ───────────────────────────────────────────────────  ─────────────────────────────────────────────  ─────────────────────────────────────────────────
   分层架构依赖方向                                     layer_violation                                单包全量扫描下价值较高，可门禁
  ───────────────────────────────────────────────────  ─────────────────────────────────────────────  ─────────────────────────────────────────────────
   MQTT/BLE/业务模块隔离                                module_violation                               配置完整后可门禁
  ───────────────────────────────────────────────────  ─────────────────────────────────────────────  ─────────────────────────────────────────────────
   文件级循环 import                                    circular_dependency                            全量扫描可作提示，不建议 changed-only 门禁
  ───────────────────────────────────────────────────  ─────────────────────────────────────────────  ─────────────────────────────────────────────────
   Timer、Controller、Subscription 未释放               lifecycle_resource_not_disposed                能发现直接、显式字段场景，建议报告模式
  ───────────────────────────────────────────────────  ─────────────────────────────────────────────  ─────────────────────────────────────────────────
   Widget 缺少 const 构造函数                           missing_const_constructor                      基础规范提示，不建议作为核心门禁
  ───────────────────────────────────────────────────  ─────────────────────────────────────────────  ─────────────────────────────────────────────────
   明显的设备初始化/销毁方法缺失                        device_lifecycle                               仅按方法名配对，建议代码评审线索
  ───────────────────────────────────────────────────  ─────────────────────────────────────────────  ─────────────────────────────────────────────────
   MQTT URL、连接/订阅方法配对                          mqtt_connection                                URL 粗筛有用，连接生命周期准确度有限
  ───────────────────────────────────────────────────  ─────────────────────────────────────────────  ─────────────────────────────────────────────────
   BLE 扫描停止、连接断开、超时关键词                   ble_scanning                                   适合特定 wrapper 命名规范，建议提示模式
  ───────────────────────────────────────────────────  ─────────────────────────────────────────────  ─────────────────────────────────────────────────
   硬编码凭据、明文 MQTT/HTTP、显式不安全 BLE 关键词    iot_security                                   可作 smoke check，必须允许 baseline/suppression
  ───────────────────────────────────────────────────  ─────────────────────────────────────────────  ─────────────────────────────────────────────────
   依赖版本和废弃包                                     pubspec_security                               当前标准项目布局下基本失效，不应使用

  最有价值的使用方式

  1. 架构治理

     为 presentation/domain/data/core 或 mqtt/ble/shared 配置明确 glob 和 allowed_deps，用全量扫描阻止跨层、跨模块 import。这是当前最有差异化的能力。

  2. 代码规模治理

     根据团队现状设置文件、类和 build() 阈值，用于识别持续膨胀的模块和 Widget。

  3. 显式资源泄漏初筛

     可以发现直接声明的 Timer、StreamSubscription、Controller、MqttClient、BluetoothDevice 等字段未在本类 dispose() 中直接释放的情况。

  4. IoT 安全反模式筛查

     可快速发现源码中的 password = "..."、tcp://、1883 端口和非 localhost 的 http://。适合提醒，不能证明系统安全。

  5. 存量项目渐进式接入

     baseline、源码 suppression、JSON/SARIF、--fail-on 和 --min-score 已形成完整闭环，适合先记录历史问题，再限制新增问题。

  不适合直接依赖的场景

  - CVE、真实依赖漏洞或传递依赖扫描。
  - TLS 证书校验、密钥存储安全、权限合规。
  - 跨方法、跨类、继承、mixin、DI 场景的资源生命周期证明。
  - MQTT/BLE API 调用图、异步异常路径或真实连接状态分析。
  - 运行时性能、功耗、内存泄漏检测。
  - 整个 Melos monorepo 的 package graph 分析。
  - 仅依赖 --changed-only 作为合并门禁。

  MQTT、BLE 和设备生命周期规则目前检查的主要是方法声明名称，而不是 _client.connect()、startScan() 等真实调用关系；安全规则也会扫描注释、示例和测试字符串。这些规则默认
  直接阻断 high 风险会产生噪声。

  推荐落地策略

  - PR：changed-only 用于快速提示。
  - 主干、夜间、发布前：执行全量扫描。
  - 硬门禁：优先启用经过 config doctor 验证的 layer/module 规则和规模规则。
  - IoT、生命周期、安全规则：先报告和 baseline，积累真实误报数据后再决定是否参与门禁。
  - 架构硬门禁建议使用独立配置，关闭尚未校准的 device_lifecycle、mqtt_connection、ble_scanning 和 iot_security。

  当前更准确的产品定位是：Flutter/IoT 项目的架构与反模式治理 CLI，而不是完整的 Dart analyzer 或安全扫描平台。
