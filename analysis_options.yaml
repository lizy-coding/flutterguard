include: package:lints/recommended.yaml

analyzer:
  exclude:
    - test/fixtures/**
  language:
    strict-casts: true
    strict-inference: true

linter:
  rules:
    - avoid_dynamic_calls
    - prefer_const_declarations
    - prefer_final_locals
    - unawaited_futures
